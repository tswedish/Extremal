[package]
name = "ramseynet-types"
version = "0.1.0"
edition = "2021"
description = "Shared types for the RamseyNet protocol"
license = "MIT"

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
hex = { workspace = true }
