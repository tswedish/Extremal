Feature Request Backlog:
- Web App for workers, see stats and running status, control and change strategies
    - Maybe there's a generic worker interface, which the webapp supports, and various "engines" can be attached to it? This would enable things like multithreaded CPU, GPU accelerated algorithms, and such. Engines can expose controls and metrics for the webapp to display. As such engines become a kind of plugin.
- Rename project to reflect the graph "Gem" concept. maybe "GraphGem"?
    - Note that while Ramsey Graphs represent some of the rarest graphs, a Gem can be found for other types of graphs
    - It's really the clique size and number of cliques that matter in terms of rareness and quality of gems
    - Perhaps there are categories of rareness, in in RPGs, where we have gray (super common), white (common), green (uncommon), blue (rare / ramsey graphs), purple (critical ramsey graphs), epic (critical maximal in rarity in terms of properties). This are just suggestions. 
    - the RPG style categories could be enchanced by geological metaphors, with like rocks, crystals, minerals, ores, etc.